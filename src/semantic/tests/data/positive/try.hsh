function oops(ill)
	if ill then
		std.error("something went wrong", nil)
	else
		"it works!"
	end
end


function handle()
	let first = oops(false)?
	std.println(first)

	let second = oops(true)?
	std.println(second)
end
